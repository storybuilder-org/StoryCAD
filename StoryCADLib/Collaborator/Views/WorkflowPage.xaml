<?xml version="1.0" encoding="utf-8"?>

<!--
    CROSS-PLATFORM BINDING PATTERN

    Why we use {x:Bind ViewModel.Property} instead of {x:Bind Property} with x:DataType:

    1. x:DataType at Page level is NOT supported on Skia/Desktop (macOS) targets.
       Windows/WinUI supports it, but Skia generates: "Property x:DataType is not available"

    2. x:Bind at Page level binds to the PAGE CLASS, not DataContext.
       Per Uno docs: "For top level controls the data context is the control itself"

    SOLUTION: Expose a public ViewModel property in code-behind that returns DataContext,
    then bind to ViewModel.Property. This works on ALL platforms.

    The [ForceUpdate(true)] attribute on the code-behind class tells Uno.Extensions.Navigation
    to call Bindings.Update() when navigating, ensuring bindings refresh when DataContext changes.

    See: https://github.com/unoplatform/uno.extensions/issues/2363

    Layout (#129): work and chat are independent columns so the chat input row does not
    leave a blank band under Property Updates. Each column owns its own bottom Auto row.
-->
<Page
    x:Class="StoryCADLib.Collaborator.Views.WorkflowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:StoryCADLib.Collaborator.Views"
    mc:Ignorable="d"
    VerticalAlignment="Stretch"
    HorizontalAlignment="Stretch">
    <Page.Resources>
        <!-- Chat bubble: sender label shows once per run of same-sender messages (#129).
             Bubbles stretch to the full chat column width rather than sitting at a fixed
             420px cap. The speaker is still cued by the bubble colour and by the sender
             label, which keeps its left/right alignment. -->
        <DataTemplate x:Key="ChatBubbleTemplate">
            <!-- Template root: no AutomationId (TemplateSafety forbids it). -->
            <StackPanel Margin="0,4,0,4"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.Name="{Binding Text, Mode=OneWay}">
                <TextBlock Text="{Binding Sender}"
                           FontWeight="SemiBold"
                           FontSize="11"
                           Opacity="0.7"
                           Visibility="{Binding SenderVisibility}"
                           HorizontalAlignment="{Binding BubbleAlignment}" />
                <Border CornerRadius="12"
                        Padding="12,8,12,8"
                        Margin="0,2,0,0"
                        Background="{Binding BackgroundBrush}"
                        HorizontalAlignment="Stretch">
                    <TextBlock Text="{Binding Text}"
                               TextWrapping="Wrap"
                               Foreground="{Binding TextBrush}" />
                </Border>
            </StackPanel>
        </DataTemplate>

        <!-- Rolled-up workflow status lines: one collapsed expander per run (#129). -->
        <DataTemplate x:Key="ChatStatusTemplate">
            <Expander Margin="0,4,0,4"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      IsExpanded="False"
                      AutomationProperties.Name="{Binding StatusHeader, Mode=OneWay}">
                <Expander.Header>
                    <TextBlock Text="{Binding StatusHeader}"
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                </Expander.Header>
                <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       FontSize="11"
                                       TextWrapping="Wrap"
                                       Margin="0,1"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>
        </DataTemplate>

        <views:ChatMessageTemplateSelector x:Key="ChatTemplateSelector"
                                           Bubble="{StaticResource ChatBubbleTemplate}"
                                           Status="{StaticResource ChatStatusTemplate}" />
    </Page.Resources>
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- 3:2 split — the table needs the width more than the chat does. -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- ========== Work column (full height; Property Updates takes leftover) ========== -->
        <Grid Grid.Column="0"
              Margin="0,0,12,0"
              VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Brief purpose + topical Explanation (selected elements + what to do next) -->
            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           MaxLines="2"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{x:Bind ViewModel.Explanation, Mode=OneWay}"
                           TextWrapping="Wrap"
                           Margin="0,6,0,0"
                           MaxLines="4"
                           TextTrimming="CharacterEllipsis"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Property Updates: fills remaining height; always show scrollbar when list scrolls -->
            <Border Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="14"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch">
                <Grid VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <!-- #119: the section picker owns the left panel until the interview
                         starts, then the Property Updates list takes it back. It sits over
                         all three rows because there are no proposals to show yet. -->
                    <Grid Grid.Row="0" Grid.RowSpan="3"
                          Visibility="{x:Bind ViewModel.InterviewPickerVisibility, Mode=OneWay}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Directions before the list (Terry, 2026-08-11: the picker
                             needs "some general directions at the start"). The rows below
                             are phrased as they will be put to the character, so the
                             framing has to say who is being addressed. -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="Interview this character"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,4" />
                            <TextBlock TextWrapping="Wrap"
                                       Opacity="0.8"
                                       FontSize="12"
                                       Text="Tick the parts of their life you want to ask about. They answer in their own voice, one section at a time, and you can break in with a question of your own whenever you like. Nothing reaches your outline until you press Summarize." />
                        </StackPanel>

                        <ScrollViewer Grid.Row="1"
                                      VerticalScrollBarVisibility="Visible"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.InterviewSections, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- Template root: no AutomationId (TemplateSafety);
                                             the accessible name binds instead. -->
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  Margin="0,4"
                                                  AutomationProperties.Name="{Binding Title, Mode=OneWay}">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title, Mode=OneWay}" />
                                                <TextBlock Text="{Binding Blurb, Mode=OneWay}"
                                                           Opacity="0.7"
                                                           TextWrapping="Wrap"
                                                           FontSize="12" />
                                            </StackPanel>
                                        </CheckBox>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <StackPanel Grid.Row="2" Margin="0,8,0,0">
                            <Button Content="Start interview"
                                    HorizontalAlignment="Stretch"
                                    Command="{x:Bind ViewModel.StartInterviewCommand}"
                                    IsEnabled="{x:Bind ViewModel.HasChosenSections, Mode=OneWay}"
                                    AutomationProperties.AutomationId="StartInterviewButton" />
                            <!-- Never gated on a tick: the point is to skip the script. -->
                            <Button Content="Skip to my own questions"
                                    Margin="0,6,0,0"
                                    HorizontalAlignment="Stretch"
                                    Command="{x:Bind ViewModel.SkipToQuestionsCommand}"
                                    AutomationProperties.AutomationId="SkipToQuestionsButton" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Grid.Row="0"
                               Visibility="{x:Bind ViewModel.PropertyUpdatesVisibility, Mode=OneWay}"
                               Text="{x:Bind ViewModel.PendingUpdatesHeader, Mode=OneWay}"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />

                    <!-- No column headers: the panel title names the table and the rows label
                         themselves. What is left is the rule that separates title from list.
                         Right margin matches the gutter the built-in scrollbar reserves
                         (ScrollBarSize, 12px) so the rule ends level with the rows (#129). -->
                    <Border Grid.Row="1"
                            Height="1"
                            Margin="0,0,12,0"
                            Visibility="{x:Bind ViewModel.PropertyUpdatesVisibility, Mode=OneWay}"
                            Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                    <!--
                      VerticalScrollBarVisibility must be Visible, never Disabled: per WinUI's
                      ScrollBarVisibility contract, Disabled also turns scrolling off and clamps
                      content height to the viewport, so rows clip and the wheel does nothing
                      (VerticalScrollMode does not override it). Visible keeps the built-in bar
                      on screen whenever the list overflows: the scroll affordance #129 asks for.
                    -->
                    <!-- ScrollViewer: no AutomationId (not in convention suffix table). List content is not a ListView. -->
                    <ScrollViewer Grid.Row="2"
                                  VerticalAlignment="Stretch"
                                  Visibility="{x:Bind ViewModel.PropertyUpdatesVisibility, Mode=OneWay}"
                                  VerticalScrollBarVisibility="Visible"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.PendingUpdateItems, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- Template root: no AutomationId (TemplateSafety forbids it).
                                             Column widths match the header row above the list. -->
                                        <Grid Padding="0,8"
                                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                              BorderThickness="0,0,0,1"
                                              AutomationProperties.Name="{Binding Key, Mode=OneWay}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1*" />
                                                <ColumnDefinition Width="3*" />
                                                <ColumnDefinition Width="64" />
                                            </Grid.ColumnDefinitions>
                                            <!-- Tooltip carries the raw key so the element is still discoverable
                                                 without its own column. KindLabel sits under the property name
                                                 rather than in a column of its own. -->
                                            <StackPanel VerticalAlignment="Top"
                                                        Margin="0,0,8,0">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontWeight="SemiBold"
                                                           TextWrapping="Wrap"
                                                           ToolTipService.ToolTip="{Binding Key}" />
                                                <TextBlock Text="{Binding KindCaption}"
                                                           FontSize="11"
                                                           Opacity="0.6"
                                                           TextWrapping="Wrap"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding ProposedDisplay}"
                                                       Opacity="0.9"
                                                       VerticalAlignment="Top"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,8,0" />
                                            <StackPanel Grid.Column="2"
                                                        Orientation="Horizontal"
                                                        Spacing="4"
                                                        VerticalAlignment="Top">
                                                <Button Click="AcceptItemButton_Click"
                                                        Padding="5"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        ToolTipService.ToolTip="Accept this update"
                                                        AutomationProperties.Name="Accept update">
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="&#xE73E;" FontSize="12" />
                                                </Button>
                                                <Button Click="SkipItemButton_Click"
                                                        Padding="5"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        ToolTipService.ToolTip="Skip this update"
                                                        AutomationProperties.Name="Skip update">
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="&#xE711;" FontSize="12" />
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Review Mode (Accept/Skip); Accept All sits below the table -->
            <StackPanel Grid.Row="2"
                        Visibility="{x:Bind ViewModel.ReviewModeVisibility, Mode=OneWay}"
                        Margin="0,8,0,0">
                <TextBlock Text="Review Property Update" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Border Background="{ThemeResource SystemAccentColorLight2}"
                        CornerRadius="4" Padding="10" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.CurrentReviewDisplayName, Mode=OneWay}"
                                   FontWeight="Bold" FontSize="13" />
                        <TextBlock Text="Yours"
                                   FontSize="11"
                                   Margin="0,8,0,0"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.CurrentReviewExisting, Mode=OneWay}"
                                   TextWrapping="Wrap" Margin="0,2,0,0" />
                        <TextBlock Text="Proposed"
                                   FontSize="11"
                                   Margin="0,8,0,0"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.CurrentReviewValue, Mode=OneWay}"
                                   TextWrapping="Wrap" Margin="0,2,0,0" />
                        <TextBlock Text="{x:Bind ViewModel.CurrentReviewCraft, Mode=OneWay}"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0"
                                   FontSize="12"
                                   Visibility="{x:Bind ViewModel.CurrentReviewCraftVisibility, Mode=OneWay}" />
                    </StackPanel>
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="8">
                    <Button Content="Accept"
                            Command="{x:Bind ViewModel.AcceptCurrentCommand}"
                            IsEnabled="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay}"
                            Style="{ThemeResource AccentButtonStyle}"
                            AutomationProperties.AutomationId="WorkflowAcceptButton" />
                    <Button Content="Skip"
                            Command="{x:Bind ViewModel.SkipCurrentCommand}"
                            IsEnabled="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay}"
                            AutomationProperties.AutomationId="WorkflowSkipButton" />
                    <Button Content="Accept Free Remaining"
                            Command="{x:Bind ViewModel.AcceptRemainingCommand}"
                            IsEnabled="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay}"
                            AutomationProperties.AutomationId="WorkflowAcceptRemainingButton"
                            ToolTipService.ToolTip="Applies empty/session fields only; leaves fields that already have your text" />
                </StackPanel>
                <TextBlock Text="{x:Bind ViewModel.ReviewProgress, Mode=OneWay}"
                           Margin="0,8,0,0"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Accept All: primary action for the table, at the foot of the work column.
                 Accepted updates are written to the outline immediately, so there is no
                 separate Save button on the shell bar. -->
            <StackPanel Grid.Row="3">
                <Button Content="Accept all changes"
                        Command="{x:Bind ViewModel.AcceptAllCommand}"
                        IsEnabled="{x:Bind ViewModel.HasPendingUpdates, Mode=OneWay}"
                        Style="{ThemeResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Margin="0,10,0,0"
                        AutomationProperties.AutomationId="WorkflowAcceptAllButton"
                        AutomationProperties.Name="Accept all changes" />

                <!-- #119: interview controls, collapsed outside an interview. Leaving them
                     disabled instead would put a dead pair of buttons under Accept all on
                     every other workflow page, and the taller row would permanently shorten
                     the Property Updates list above it. -->
                <StackPanel Orientation="Horizontal"
                            Margin="0,8,0,0"
                            Visibility="{x:Bind ViewModel.InterviewControlsVisibility, Mode=OneWay}">
                    <Button Content="{x:Bind ViewModel.NextSectionLabel, Mode=OneWay}"
                            Command="{x:Bind ViewModel.NextSectionCommand}"
                            IsEnabled="{x:Bind ViewModel.CanAskNextSection, Mode=OneWay}"
                            AutomationProperties.AutomationId="NextInterviewSectionButton" />
                    <Button Content="Summarize"
                            Margin="8,0,0,0"
                            Command="{x:Bind ViewModel.SummarizeCommand}"
                            IsEnabled="{x:Bind ViewModel.CanSummarize, Mode=OneWay}"
                            AutomationProperties.AutomationId="SummarizeInterviewButton" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- ========== Chat column (own height; input does not affect work column) ========== -->
        <Grid Grid.Column="1"
              Margin="12,0,0,0"
              VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView Grid.Row="0"
                      ItemsSource="{x:Bind ViewModel.ConversationList, Mode=OneWay}"
                      Margin="0,0,0,10"
                      VerticalAlignment="Stretch"
                      SelectionMode="None"
                      HorizontalContentAlignment="Stretch"
                      ItemTemplateSelector="{StaticResource ChatTemplateSelector}"
                      AutomationProperties.AutomationId="WorkflowConversationList">
                <!-- Containers must stretch, else the bubble template only ever gets its
                     desired width and stretching inside the template does nothing. The
                     stock ListViewItem padding/MinHeight would also inset every bubble. -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel VerticalAlignment="Bottom"
                                         ItemsUpdatingScrollMode="KeepLastItemInView" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>

            <!-- No suggestion items: used for its Enter-submits behavior and in-box send button.
                 No Header/Placeholder: explicit Name (chat message input). -->
            <AutoSuggestBox x:Name="InputTextBox"
                            Grid.Row="1"
                            HorizontalAlignment="Stretch"
                            IsEnabled="{x:Bind ViewModel.IsChatEnabled, Mode=OneWay}"
                            PlaceholderText="{x:Bind ViewModel.ChatPlaceholder, Mode=OneWay}"
                            Text="{x:Bind ViewModel.InputText, Mode=TwoWay}"
                            QuerySubmitted="InputTextBox_QuerySubmitted"
                            Loaded="InputTextBox_Loaded"
                            AutomationProperties.AutomationId="InputSearchBox"
                            AutomationProperties.Name="Message">
                <AutoSuggestBox.QueryIcon>
                    <SymbolIcon Symbol="Send" />
                </AutoSuggestBox.QueryIcon>
            </AutoSuggestBox>
        </Grid>

        <!-- Progress over both columns, bottom edge of page -->
        <ProgressBar x:Name="ResponseProgressBar"
                     Grid.Column="0" Grid.ColumnSpan="2"
                     VerticalAlignment="Bottom"
                     Height="5" IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.ProgressVisibility, Mode=TwoWay}" />
    </Grid>
</Page>
