<Page
    x:Class="StoryCAD.Services.Dialogs.FileOpenMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Height="500" Width="800">

    <NavigationView OverflowLabelMode="NoLabel" IsBackButtonVisible="Collapsed" SelectedItem="{x:Bind UnifiedMenuVM.CurrentTab, Mode=TwoWay}"
                    IsSettingsVisible="False" PaneDisplayMode="Left" IsPaneToggleButtonVisible="False">
        <NavigationView.MenuItems>
            <!-- New button -->
            <NavigationViewItem Content="Create new outline" Tag="New">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Recents button -->
            <NavigationViewItem Content="Recently opened" Tag="Recent">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xED25;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Samples button -->
            <NavigationViewItem Content="Sample outlines" Tag="Sample">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE753;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Grid Margin="10" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title and warning -->
            <TextBlock Text="{x:Bind UnifiedMenuVM.TitleText, Mode=TwoWay}" FontSize="25" HorizontalAlignment="Center"
                       VerticalAlignment="Top" Grid.Row="0"/>

            <InfoBar Grid.Row="1" IsOpen="{x:Bind UnifiedMenuVM.ShowWarning, Mode=TwoWay}" Severity="Warning" IsClosable="False"
                     Message="{x:Bind UnifiedMenuVM.WarningText, Mode=TwoWay}" HorizontalAlignment="Center"/>

            <!-- Recents tab -->
            <Grid Visibility="{x:Bind UnifiedMenuVM.RecentsTabContentVisibilty, Mode=TwoWay}" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <ListBox Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" 
                         ItemsSource="{x:Bind UnifiedMenuVM.RecentlyOpenedFiles}"
                         SelectionChanged="{x:Bind UnifiedMenuVM.LoadRecentStory}"
                         SelectedIndex="{x:Bind UnifiedMenuVM.SelectedRecentIndex, Mode=TwoWay}"/>
            </Grid>

            <!-- New tab -->
            <Grid Visibility="{x:Bind UnifiedMenuVM.NewTabContentVisibilty, Mode=TwoWay}" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ComboBox Grid.Row="0" Header="Template:" HorizontalAlignment="Stretch" Margin="20"
                          SelectedIndex="{x:Bind UnifiedMenuVM.SelectedTemplateIndex, Mode=TwoWay}">
                    <x:String>Blank Outline</x:String>
                    <x:String>Overview and Story Problem</x:String>
                    <x:String>Folders</x:String>
                    <x:String>External and Internal Problems</x:String>
                    <x:String>Protagonist and Antagonist</x:String>
                    <x:String>Problems and Characters</x:String>
                </ComboBox>

                <TextBox Grid.Row="1" HorizontalContentAlignment="Center" PlaceholderText="You can't put files here"
                             Header="Project path:"  HorizontalAlignment="Stretch" Margin="20"
                             Text="{x:Bind UnifiedMenuVM.ProjectPath, Mode=TwoWay}"/>

                <Button Grid.Row="2" Content="Browse" Click="{x:Bind UnifiedMenuVM.Browse_Click}" HorizontalAlignment="Center"/>

                <TextBox Grid.Row="3" MinWidth="250"  HorizontalAlignment="Stretch" Margin="20"
                             Header="Project Name: " Text="{x:Bind UnifiedMenuVM.ProjectName, Mode=TwoWay}"/>
            </Grid>

            <!-- Samples tab -->
            <Grid Visibility="{x:Bind UnifiedMenuVM.SamplesTabContentVisibilty, Mode=TwoWay}" Grid.Row="2">

                <ListBox HorizontalAlignment="Center" VerticalAlignment="Center"
                         ItemsSource="{x:Bind UnifiedMenuVM.SampleNames}" CornerRadius="4"
                         SelectedIndex="{x:Bind UnifiedMenuVM.SelectedSampleIndex}"/>
            </Grid>

            <!-- Footer buttons -->
            <Grid Grid.Row="3" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MinWidth="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition MinWidth="10"/>
                </Grid.ColumnDefinitions>

                <!-- buttons -->
                <Button Grid.Column="1" Content="{x:Bind UnifiedMenuVM.ConfirmButtonText, Mode=TwoWay}"
                        Click="{x:Bind UnifiedMenuVM.ConfirmClicked}" HorizontalAlignment="Stretch" />
                <Button Grid.Column="2" Content="Close" HorizontalAlignment="Stretch" Click="{x:Bind UnifiedMenuVM.Close}"/>
            </Grid>
        </Grid>
    </NavigationView>
</Page>