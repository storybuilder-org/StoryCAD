<Page
    x:Class="StoryCADLib.Services.Dialogs.FileOpenMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" MinWidth="360" Background="Transparent">

    <NavigationView HorizontalContentAlignment="Stretch" OverflowLabelMode="NoLabel" IsBackButtonVisible="Collapsed"
                    SelectedItem="{x:Bind FileOpenVM.CurrentTab, Mode=TwoWay}"
                    IsSettingsVisible="False" PaneDisplayMode="Left" IsPaneToggleButtonVisible="False"
                    Background="Transparent" BorderThickness="0">
        <NavigationView.MenuItems>
            <!-- New button -->
            <NavigationViewItem Content="Create new outline" Tag="New">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE710;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Recents button -->
            <NavigationViewItem Content="Recently opened" Tag="Recent">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xEC92;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Samples button -->
            <NavigationViewItem Content="Sample outlines" Tag="Sample">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE753;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Backup button -->
            <NavigationViewItem Content="Backups" Tag="Backup">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE777;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Open from file -->
            <NavigationViewItem Content="Open from file" Tag="OpenFile">
                <NavigationViewItem.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE838;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <!-- Settings/Preferences section at the bottom -->
            <NavigationViewItemSeparator />
            <NavigationViewItem IsEnabled="True" SelectsOnInvoked="False">
                <NavigationViewItem.Content>
                    <CheckBox Content="Show on startup"
                              IsChecked="{x:Bind FileOpenVM.ShowFilePickerOnStartup, Mode=TwoWay}"
                              Margin="0,0,10,0" />
                </NavigationViewItem.Content>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Grid Margin="10" Background="Transparent" BorderThickness="0" MinWidth="350" MinHeight="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title and warning -->
            <TextBlock Text="{x:Bind FileOpenVM.TitleText, Mode=TwoWay}" FontSize="25" HorizontalAlignment="Center"
                       VerticalAlignment="Top" Grid.Row="0" />

            <InfoBar Grid.Row="1" IsOpen="{x:Bind FileOpenVM.ShowWarning, Mode=TwoWay}" Severity="Warning"
                     IsClosable="False"
                     Message="{x:Bind FileOpenVM.WarningText, Mode=TwoWay}" HorizontalAlignment="Center" />

            <ScrollView Grid.Row="2" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="Transparent">
                <Grid Background="Transparent" BorderThickness="0">
                    <!-- Recents tab -->
                    <Grid Visibility="{x:Bind FileOpenVM.RecentsTabContentVisibility, Mode=OneWay}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ListView Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                  ItemsSource="{x:Bind FileOpenVM.RecentsUI}"
                                  SelectedIndex="{x:Bind FileOpenVM.SelectedRecentIndex, Mode=TwoWay}">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel Orientation="Vertical" Margin="0,10,0,0" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                        </ListView>
                    </Grid>

                    <!-- Backups tab -->
                    <Grid Visibility="{x:Bind FileOpenVM.BackupTabContentVisibility, Mode=OneWay}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ListView Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                 ItemsSource="{x:Bind FileOpenVM.BackupUI}"
                                 SelectedIndex="{x:Bind FileOpenVM.SelectedBackupIndex, Mode=TwoWay}" />
                    </Grid>

                    <!-- New tab -->
                    <Grid Visibility="{x:Bind FileOpenVM.NewTabContentVisibility, Mode=OneWay}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ComboBox Grid.Row="0" Header="Template:" HorizontalAlignment="Stretch" Margin="20"
                                  ItemsSource="{x:Bind FileOpenVM.ProjectTemplateNames}"
                                  SelectedIndex="{x:Bind FileOpenVM.SelectedTemplateIndex, Mode=TwoWay}" />

                        <TextBox Grid.Row="1" HorizontalContentAlignment="Center"
                                 PlaceholderText="You can't put files here"
                                 Header="Project path:" HorizontalAlignment="Stretch" Margin="20"
                                 Text="{x:Bind FileOpenVM.OutlineFolder, Mode=TwoWay}" />

                        <Button Grid.Row="2" Content="Browse" Click="{x:Bind FileOpenVM.Browse_Click}"
                                HorizontalAlignment="Center" />

                        <TextBox Grid.Row="3" MinWidth="250" HorizontalAlignment="Stretch" Margin="20"
                                 Header="Project Name: " Text="{x:Bind FileOpenVM.OutlineName, Mode=TwoWay}" />
                    </Grid>

                    <!-- Samples tab -->
                    <Grid Visibility="{x:Bind FileOpenVM.SamplesTabContentVisibility, Mode=OneWay}">
                        <ListView HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                  ItemsSource="{x:Bind FileOpenVM.SampleNames}" CornerRadius="4"
                                  SelectedIndex="{x:Bind FileOpenVM.SelectedSampleIndex, Mode=TwoWay}" />
                    </Grid>
                </Grid>
            </ScrollView>


        </Grid>
    </NavigationView>
</Page>
