<Page
    x:Class="StoryCADLib.Services.Dialogs.SaveAsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:StoryCADLib.Controls"
    mc:Ignorable="d">

    <StackPanel Padding="14,14,14,24">
        <TextBox x:Name="ProjectName" Margin="10" x:FieldModifier="public" Width="400"
                 HorizontalAlignment="Left" Header="New project Name" Text="{x:Bind SaveAsVm.ProjectName, Mode=TwoWay}"
                 AutomationProperties.AutomationId="SaveAsProjectNameTextBox" />
        <!-- Single BrowseTextBox instance on this page with a Header set, so the Header already
             reaches the inner TextBox's UIA Name; an explicit outer Name is only needed to
             disambiguate multiple same-type instances on one screen (see PreferencesInitialization). -->
        <controls:BrowseTextBox
            x:Name="ProjectPathBrowse"
            Margin="14"
            Header="Parent Folder Path"
            Path="{x:Bind SaveAsVm.ParentFolder, Mode=TwoWay}"
            TextBoxWidth="330"
            BrowseMode="Folder"
            ButtonVerticalAlignment="Bottom"
            ButtonMargin="0,0,0,0"
            PathSelected="OnPathSelected"
            AutomationProperties.AutomationId="SaveAsParentFolderTextBox" />
    </StackPanel>
</Page>
