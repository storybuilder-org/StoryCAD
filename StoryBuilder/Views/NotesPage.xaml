<local:BindablePage NavigationCacheMode="Required"  
    x:Class="StoryBuilder.Views.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryBuilder.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="using:StoryBuilder.Controls"
    mc:Ignorable="d" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Text="{x:Bind NotesVM.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NameBox}"/>
        <usercontrols:RichEditBoxExtended Header="Notes" Grid.Row="1" RtfText="{x:Bind NotesVM.Notes, Mode=TwoWay}"  Margin="10"/>
    </Grid>   

</local:BindablePage>