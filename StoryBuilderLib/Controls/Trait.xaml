<UserControl
    x:Class="StoryBuilder.Controls.Trait"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryBuilder.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0" Header="Behaviors" IsEditable="False" MinWidth="300" ItemsSource="{x:Bind TraitVm.BehaviorList}"
                              SelectedValue="{x:Bind TraitVm.Behavior, Mode=TwoWay}" />
        <ComboBox Grid.Row="1" Header="Habits" IsEditable="False" MinWidth="300" ItemsSource="{x:Bind TraitVm.HabitList}"
                              SelectedValue="{x:Bind TraitVm.Habit, Mode=TwoWay}" />
        <ComboBox Grid.Row="2" Header="Skills and Abilities" IsEditable="False" MinWidth="300" ItemsSource="{x:Bind TraitVm.SkillList}"
                              SelectedValue="{x:Bind TraitVm.Skill, Mode=TwoWay}" />
    </Grid>
</UserControl>
