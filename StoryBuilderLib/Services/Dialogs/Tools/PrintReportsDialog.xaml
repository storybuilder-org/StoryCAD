<Page
    x:Class="StoryBuilder.Services.Dialogs.Tools.PrintReportsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryBuilder.Services.Dialogs.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:StoryBuilder.ViewModels"
    mc:Ignorable="d">

    <StackPanel Width="800" MinWidth="300">
        <StackPanel HorizontalAlignment="Center" Margin="0,15,520,15">
            <TextBlock Text="Create reports for the following:" />
            <CheckBox Content="Story Overview" IsChecked="{x:Bind PrintVM.CreateOverview, Mode=TwoWay}" />
            <CheckBox Content="Story Synopsis" IsChecked="{x:Bind PrintVM.CreateSummary, Mode=TwoWay}"/>
            <TextBlock MinWidth="50" />
            <CheckBox Content="List of Problems" IsChecked="{x:Bind PrintVM.ProblemList, Mode=TwoWay}"/>
            <CheckBox Content="List of Characters" IsChecked="{x:Bind PrintVM.CharacterList, Mode=TwoWay}"/>
            <CheckBox Content="List of Settings" IsChecked="{x:Bind PrintVM.SettingList, Mode=TwoWay}"/>
            <CheckBox Content="List of Scenes" IsChecked="{x:Bind PrintVM.SceneList, Mode=TwoWay}"/>
        </StackPanel>

        <Pivot Height="300">
            <PivotItem Header="Problems" TabIndex="0">
                <ListView SelectionMode="Multiple" Height="290" Name="ProblemsList" ItemsSource="{x:Bind PrintVM.ProblemNodes, Mode=TwoWay}" SelectionChanged="UpdateSelection" />
            </PivotItem>
            <PivotItem Header="Characters">
                <ListView SelectionMode="Multiple" Height="290" Name="CharactersList" ItemsSource="{x:Bind PrintVM.CharacterNodes, Mode=TwoWay}"  SelectionChanged="UpdateSelection"/>
            </PivotItem>
            <PivotItem Header="Scenes">
                <ListView SelectionMode="Multiple" Height="290" Name="ScenesList" ItemsSource="{x:Bind PrintVM.SceneNodes, Mode=TwoWay}"  SelectionChanged="UpdateSelection"/>
            </PivotItem>
            <PivotItem Header="Settings">
                <ListView SelectionMode="Multiple" Height="290" Name="SettingsList" ItemsSource="{x:Bind PrintVM.SettingNodes, Mode=TwoWay}"  SelectionChanged="UpdateSelection"/>
            </PivotItem>

        </Pivot>
 
    </StackPanel>
</Page>
