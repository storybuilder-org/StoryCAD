<Page
    x:Class="StoryBuilder.Services.Dialogs.RecentFiles"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryBuilder.Services.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{x:Bind UnifiedMenuVM.AdjustmentColor, Mode=OneWay}">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Center" MinHeight="500"  Background="{x:Bind UnifiedMenuVM.AdjustmentColor}" Margin="0,0,70,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Recent files: " FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Name="Pannel" VerticalAlignment="Center" HorizontalAlignment="Stretch" Width="350">
            <ListBox VerticalAlignment="Center" HorizontalAlignment="Center" Name="Recents" Grid.Row="1" SelectionChanged="{x:Bind UnifiedMenuVM.LoadRecentStory}"  SelectedIndex="{x:Bind UnifiedMenuVM.SelectedRecentIndex, Mode=TwoWay}"/>
        </StackPanel>
        <Button Content="Open story from disk" Click="{x:Bind UnifiedMenuVM.LoadStory}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="2"/>
    </Grid>
</Page>