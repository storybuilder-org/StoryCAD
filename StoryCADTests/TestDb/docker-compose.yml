services:
  mysql:
    build: .
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: stbtest
      MYSQL_PASSWORD: "123"
      MYSQL_DATABASE: StoryBuilder
    volumes:
      - storybuilder-data:/var/lib/mysql

volumes:
  storybuilder-data:
